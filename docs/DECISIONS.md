# DECISIONS.md — Decision Log

Recorded decisions only. Proposals and assumptions are NOT decisions and must not be entered here.

## Status legend

- **Decided** — confirmed decision. Do not reverse without logging a change here.
- **Open** — open question. Must be resolved deliberately in the appropriate phase, not incidentally.
- **Future** — planned implementation detail, not yet decided.

## Decision log

| # | Date | Decision | Status |
| --- | --- | --- | --- |
| 1 | 2026-08-19 | **Portfolio purpose:** A complete professional personal portfolio (represents the full professional identity, not only ML engineering). | Decided |
| 2 | 2026-08-19 | **Scope:** Projects, experience, publications, skills, education, certifications, achievements, and relevant professional identity. | Decided |
| 3 | 2026-08-19 | **Deployment:** Vercel. | Decided |
| 4 | 2026-08-19 | **Database:** None. | Decided |
| 5 | 2026-08-19 | **CMS:** None. | Decided |
| 6 | 2026-08-19 | **Architecture:** Next.js-based application with repository-managed content. | Decided |
| 7 | 2026-08-19 | **Visual ambition:** Highly interactive and visually sophisticated, inspired by premium creative portfolios. | Decided |
| 8 | 2026-08-19 | **3D:** Allowed and encouraged where it strengthens the experience (Three.js / React Three Fiber), but not everywhere. | Decided |
| 9 | 2026-08-19 | **Final sitemap:** SUPERSEDED by #14. | Decided |
| 10 | 2026-08-19 | **Final color palette:** SUPERSEDED by #16–17. | Decided |
| 11 | 2026-08-19 | **Final typography:** Not decided. | Open |
| 12 | 2026-08-19 | **Final branding / logo-mark:** Not decided. | Open |
| 13 | 2026-08-19 | **Tech stack:** Next.js, TypeScript, Tailwind CSS, GSAP + ScrollTrigger, Motion/Framer Motion, Lenis, Three.js + React Three Fiber + Drei, Lucide React, next/font, Git/GitHub, Vercel. No database, no backend, no CMS. | Decided |
| 14 | 2026-08-19 | **High-level sitemap:** HOME, WORK, ABOUT, EXPERIENCE, PUBLICATIONS, CONTACT. | Decided |
| 15 | 2026-08-19 | **RESUME + SKILLS:** RESUME is a prominent persistent action/link, not necessarily a full page. SKILLS is intentionally NOT a standalone top-level page — skills appear contextually (projects, experience, about/profile, relevant technical sections). No shallow tech-listing page. | Decided |
| 16 | 2026-08-19 | **Visual identity:** PREMIUM + TECHNICAL + PERSONAL + MODERN. Palette: **Obsidian + Champagne**, dark cinematic premium. Reference portfolio is inspiration only — not copied. | Decided |
| 17 | 2026-08-19 | **Approved color tokens:** Background `#0B0C0E`, Surface `#14161A`, Primary text `#F3F0E8`, Secondary text `#A9A9A3`, Accent `#C9A86A`, Accent highlight `#E3C98E`, Border `#2A2C30`. Accent usage ~5% of visual balance; used for interaction, emphasis, selected states, key metadata, CTA emphasis, subtle decoration. | Decided |
| 18 | 2026-08-19 | **Interaction language:** Smooth scrolling, scroll-linked reveals, sticky sections, project card stacking, subtle parallax, magnetic interactions where appropriate, hover states, page/section transitions, selective 3D. Principle: **motion serves content.** | Decided |
| 19 | 2026-08-19 | **Aesthetic guardrail:** No excessive neon, no magenta-heavy gradients, no orange/purple reference combinations, no rainbow gradients, no generic "AI neon" look, no excessive glow. Palette is architectural, premium, technical, mature, restrained. | Decided |
| 20 | 2026-08-19 | **Exact typography:** Undecided — next design decision. Do not reuse Kanit from the reference automatically. | Open |
| 21 | 2026-08-19 | **Logo / mark:** Undecided. | Open |
| 22 | 2026-08-19 | **Detailed page wireframes:** Planned in Phase 3. Interim implementation-level precedent: the Phase 4A Home milestone was implemented without formal wireframes, and its resulting layouts are documented in the ROADMAP.md Phase 4A log entries. | Future |
| 23 | 2026-08-19 | **Exact homepage composition:** Planned in Phase 3. Interim implementation-level precedent: a concrete HOME composition now exists — see ROADMAP.md Phase 4A log entries (esp. the "visual recomposition" entry). | Future |
| 24 | 2026-08-19 | **Component inventory:** Planned in Phase 3+. Interim implementation-level precedent: concrete primitives/patterns already introduced for Home (`SectionFrame`, `QuietLink`, `RevealImage`, `Interlude`, `PortraitObject`) — see ROADMAP.md Phase 4A log entries. | Future |
| 25 | 2026-08-19 | **Motion choreography (specific animations):** Planned in later phases. Interim implementation-level precedent: specific Home motion choices are logged in ROADMAP.md Phase 4A entries (masked name rise, portrait scale settle/parallax, RevealImage reveals, marquee timing, scroll-cue drift; all `prefers-reduced-motion` safe). | Future |
| 26 | 2026-08-19 | **Exact 3D concept:** Planned in later phases. Interim implementation-level precedent: the Home hero uses the CSS-3D layered portrait technique (see #28–29); the full WebGL/3D concept remains open — context in ROADMAP.md Phase 4A log entries. | Future |
| 27 | 2026-08-19 | **SaaS project rename:** The court management and booking platform is named **Movenue** (formerly Yalla7gez). Live URL: https://movenue.vercel.app/. "Yalla7gez" is not used in the implemented portfolio. | Decided |
| 28 | 2026-08-19 | **Portrait approved (Home hero):** The portrait asset `Yousef personal photo.jpeg` is approved for use and integrated into the Home hero as a dimensional, art-directed object. This resolves the previously-open portrait decision for the HOME page only; portrait use on ABOUT remains a later-milestone decision. | Decided |
| 29 | 2026-08-19 | **Home hero 3D portrait technique:** CSS 3D transforms (`perspective` + `preserve-3d`) with layered image planes — a Z-offset echo/depth layer, a masked main portrait plane (radial mask so the photo's dark background melts into Obsidian), a champagne rim-light layer, and a foreground detail layer — driven by framer-motion (idle sway + spring-smoothed pointer-follow parallax). No WebGL dependency was added: the existing stack provided a performant solution for a flat portrait asset, keeping bundle size and mobile cost low. Full WebGL/3D visual language remains a Phase 12 decision. | Decided |
| 30 | 2026-08-21 | **Home hero portrait treatment (supersedes #29; narrows the presentation described in #28 — the portrait asset itself remains approved and in the Home hero):** After user feedback that the page felt heavy and the portrait too dominant with an overdone effect, the CSS-3D layered technique is removed. The portrait is now a single flat plane at a much smaller footprint (capped ~420px on desktop, no grid bleed; no longer the ~45–50%-width hero element), kept soft-edged into Obsidian via the radial mask only. Removed entirely: echo/depth layers, rim-light/grade overlays, film grain, inner hairline frame, corner ticks, foreground light-guide/technical-readout layer, ground shadow, vertical editorial strip, "Open to work" badge (open-to-roles facts remain in the final CTA), idle sway, pointer-follow parallax, and hero scroll parallax. No WebGL then and none now. Full WebGL/3D visual language remains a Phase 12 decision. | Decided |
| 31 | 2026-08-21 | **Header/Hero typographic hierarchy (Home):** The header wordmark "Yousef Koura" becomes bold display-weight (`font-bold`, scaled up) so the header carries real typographic presence; desktop nav links group inside a subtle bordered/tinted capsule (Surface/Border tokens) with CONTACT promoted to a solid champagne CTA pill outside the capsule (RESUME still omitted — destination unresolved). To keep the two from competing, the Hero's giant repeated-name treatment is cut sharply in scale (desktop max ~10rem → ~5.75rem) so the hero's dominant weight shifts to the positioning line; existing provisional positioning copy retained unchanged pending the final statement. | Decided |
| 32 | 2026-08-21 | **Home hero portrait treatment v2 (supersedes #30; narrows it further — the portrait asset remains approved and in the Home hero):** The plain radial-masked plane is replaced by a **duotone editorial treatment** tying the photo into Obsidian + Champagne: an Obsidian gradient bed acts as the duotone shadow end, the grayscaled photo is composited via `mix-blend-luminosity` so shadows melt into Obsidian, and a low-opacity (~30%) champagne `soft-light` grade warms highlights — accent discipline maintained. Composition is an **asymmetric editorial crop**: a taller `aspect-[3/4]` frame with a single diagonal cut across the top-right corner (`clip-path` polygon), bleeding past the hero grid's right edge on desktop (`lg:-mr-6`/`xl:-mr-10`, contained by the hero's `overflow-hidden`) and rising slightly above its cell (`lg:-mt-6`); mobile stays contained and centered with the same corner cut. A **one-time CSS load reveal** (frame wipes open via inset clip-path while the plane settles from scale 1.055→1) plays once on load; afterwards the portrait is fully static — no parallax, no sway, no pointer-follow. The reveal is defined only under `prefers-reduced-motion: no-preference`; reduced-motion visitors see the final state immediately. The component keeps zero client-side JS; `priority` migrated to Next.js 16's `preload`. | Decided |
| 33 | 2026-08-21 | **Ghost-typography signature motif + evidence visualization + scroll-aware nav (Home patterns):** (a) The Interlude's giant ghost chapter word is extracted into a reusable `GhostType` primitive (oversized display type at `text-ink/[0.05]`, slow scroll drift, aria-hidden/decorative, reduce-gated). It is a recurring-but-sparse signature device with **exactly three instances on Home**: Interlude ("Profile"), Selected Work's secondary project index (ghost word "Index"), and the Profile pull-quote (ghost quotation glyph). A fourth instance requires a new logged decision. (b) One bounded interactive evidence visualization (`EvidenceChart`, Recharts) added to Selected Work: a horizontal bar comparison of best reported results — Agri-Bot 96% accuracy · PioPetro up to 87% R² · ITI up to 97% F1 · D-HUB 96% accuracy — each bar labeled with its own metric kind; "up to" qualifiers preserved verbatim. Data lives statically in `src/content/evidence.ts`, traceable to CONTENT.md CONFIRMED entries (CONTENT.md's publish-time re-verification note still applies); no network dependency, so no loading/error state is possible. Keyboard-operable via focusable row overlays; a visually-hidden data table carries values/context/sources for screen readers; entrance animation disabled under reduced motion; champagne reserved for the active/emphasized state. (c) Header gains scroll-aware nav highlighting on Home (IntersectionObserver over `#work/#about/#experience/#publications/#contact`); the highlight reuses the existing champagne active convention and is visual-only — `aria-current` remains strictly route-based. | Decided |

## Rules

- Do not treat proposals as decisions.
- Do not silently reverse a decision — record the change here with a new row and date.
- Open items (11, 12, 20–26) must be resolved deliberately in the appropriate phase per [ROADMAP.md](./ROADMAP.md) (typography/logo → Phase 5 Brand System; wireframes/homepage composition → Phase 4; component inventory → Phase 6; motion choreography/3D concept → Phase 12), not incidentally during implementation.