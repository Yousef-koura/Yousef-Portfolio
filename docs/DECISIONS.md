# DECISIONS.md — Decision Log

Recorded decisions only. Proposals and assumptions are NOT decisions and must not be entered here.

## Status legend

- **Decided** — confirmed decision. Do not reverse without logging a change here.
- **Open** — open question. Must be resolved deliberately in the appropriate phase, not incidentally.
- **Future** — planned implementation detail, not yet decided.

## Decision log

| # | Date | Decision | Status |
| --- | --- | --- | --- |
| 1 | 2026-08-19 | **Portfolio purpose:** A complete professional personal portfolio (represents the full professional identity, not only ML engineering). | Decided |
| 2 | 2026-08-19 | **Scope:** Projects, experience, publications, skills, education, certifications, achievements, and relevant professional identity. | Decided |
| 3 | 2026-08-19 | **Deployment:** Vercel. | Decided |
| 4 | 2026-08-19 | **Database:** None. | Decided |
| 5 | 2026-08-19 | **CMS:** None. | Decided |
| 6 | 2026-08-19 | **Architecture:** Next.js-based application with repository-managed content. | Decided |
| 7 | 2026-08-19 | **Visual ambition:** Highly interactive and visually sophisticated, inspired by premium creative portfolios. | Decided |
| 8 | 2026-08-19 | **3D:** Allowed and encouraged where it strengthens the experience (Three.js / React Three Fiber), but not everywhere. | Decided |
| 9 | 2026-08-19 | **Final sitemap:** SUPERSEDED by #14. | Decided |
| 10 | 2026-08-19 | **Final color palette:** SUPERSEDED by #16–17. | Decided |
| 11 | 2026-08-19 | **Final typography:** Not decided. | Open |
| 12 | 2026-08-19 | **Final branding / logo-mark:** Not decided. | Open |
| 13 | 2026-08-19 | **Tech stack:** Next.js, TypeScript, Tailwind CSS, GSAP + ScrollTrigger, Motion/Framer Motion, Lenis, Three.js + React Three Fiber + Drei, Lucide React, next/font, Git/GitHub, Vercel. No database, no backend, no CMS. | Decided |
| 14 | 2026-08-19 | **High-level sitemap:** HOME, WORK, ABOUT, EXPERIENCE, PUBLICATIONS, CONTACT. | Decided |
| 15 | 2026-08-19 | **RESUME + SKILLS:** RESUME is a prominent persistent action/link, not necessarily a full page. SKILLS is intentionally NOT a standalone top-level page — skills appear contextually (projects, experience, about/profile, relevant technical sections). No shallow tech-listing page. | Decided |
| 16 | 2026-08-19 | **Visual identity:** PREMIUM + TECHNICAL + PERSONAL + MODERN. Palette: **Obsidian + Champagne**, dark cinematic premium. Reference portfolio is inspiration only — not copied. | Decided |
| 17 | 2026-08-19 | **Approved color tokens:** Background `#0B0C0E`, Surface `#14161A`, Primary text `#F3F0E8`, Secondary text `#A9A9A3`, Accent `#C9A86A`, Accent highlight `#E3C98E`, Border `#2A2C30`. Accent usage ~5% of visual balance; used for interaction, emphasis, selected states, key metadata, CTA emphasis, subtle decoration. | Decided |
| 18 | 2026-08-19 | **Interaction language:** Smooth scrolling, scroll-linked reveals, sticky sections, project card stacking, subtle parallax, magnetic interactions where appropriate, hover states, page/section transitions, selective 3D. Principle: **motion serves content.** | Decided |
| 19 | 2026-08-19 | **Aesthetic guardrail:** No excessive neon, no magenta-heavy gradients, no orange/purple reference combinations, no rainbow gradients, no generic "AI neon" look, no excessive glow. Palette is architectural, premium, technical, mature, restrained. | Decided |
| 20 | 2026-08-19 | **Exact typography:** Undecided — next design decision. Do not reuse Kanit from the reference automatically. | Open |
| 21 | 2026-08-19 | **Logo / mark:** Undecided. | Open |
| 22 | 2026-08-19 | **Detailed page wireframes:** Planned in Phase 3. Interim implementation-level precedent: the Phase 4A Home milestone was implemented without formal wireframes, and its resulting layouts are documented in the ROADMAP.md Phase 4A log entries. | Future |
| 23 | 2026-08-19 | **Exact homepage composition:** Planned in Phase 3. Interim implementation-level precedent: a concrete HOME composition now exists — see ROADMAP.md Phase 4A log entries (esp. the "visual recomposition" entry). | Future |
| 24 | 2026-08-19 | **Component inventory:** Planned in Phase 3+. Interim implementation-level precedent: concrete primitives/patterns already introduced for Home (`SectionFrame`, `QuietLink`, `RevealImage`, `Interlude`, `PortraitObject`) — see ROADMAP.md Phase 4A log entries. | Future |
| 25 | 2026-08-19 | **Motion choreography (specific animations):** Planned in later phases. Interim implementation-level precedent: specific Home motion choices are logged in ROADMAP.md Phase 4A entries (masked name rise, portrait scale settle/parallax, RevealImage reveals, marquee timing, scroll-cue drift; all `prefers-reduced-motion` safe). | Future |
| 26 | 2026-08-19 | **Exact 3D concept:** Planned in later phases. Interim implementation-level precedent: the Home hero uses the CSS-3D layered portrait technique (see #28–29); the full WebGL/3D concept remains open — context in ROADMAP.md Phase 4A log entries. | Future |
| 27 | 2026-08-19 | **SaaS project rename:** The court management and booking platform is named **Movenue** (formerly Yalla7gez). Live URL: https://movenue.vercel.app/. "Yalla7gez" is not used in the implemented portfolio. | Decided |
| 28 | 2026-08-19 | **Portrait approved (Home hero):** The portrait asset `Yousef personal photo.jpeg` is approved for use and integrated into the Home hero as a dimensional, art-directed object. This resolves the previously-open portrait decision for the HOME page only; portrait use on ABOUT remains a later-milestone decision. | Decided |
| 29 | 2026-08-19 | **Home hero 3D portrait technique:** CSS 3D transforms (`perspective` + `preserve-3d`) with layered image planes — a Z-offset echo/depth layer, a masked main portrait plane (radial mask so the photo's dark background melts into Obsidian), a champagne rim-light layer, and a foreground detail layer — driven by framer-motion (idle sway + spring-smoothed pointer-follow parallax). No WebGL dependency was added: the existing stack provided a performant solution for a flat portrait asset, keeping bundle size and mobile cost low. Full WebGL/3D visual language remains a Phase 12 decision. | Decided |
| 30 | 2026-08-21 | **Home hero portrait treatment (supersedes #29; narrows the presentation described in #28 — the portrait asset itself remains approved and in the Home hero):** After user feedback that the page felt heavy and the portrait too dominant with an overdone effect, the CSS-3D layered technique is removed. The portrait is now a single flat plane at a much smaller footprint (capped ~420px on desktop, no grid bleed; no longer the ~45–50%-width hero element), kept soft-edged into Obsidian via the radial mask only. Removed entirely: echo/depth layers, rim-light/grade overlays, film grain, inner hairline frame, corner ticks, foreground light-guide/technical-readout layer, ground shadow, vertical editorial strip, "Open to work" badge (open-to-roles facts remain in the final CTA), idle sway, pointer-follow parallax, and hero scroll parallax. No WebGL then and none now. Full WebGL/3D visual language remains a Phase 12 decision. | Decided |
| 31 | 2026-08-21 | **Header/Hero typographic hierarchy (Home):** The header wordmark "Yousef Koura" becomes bold display-weight (`font-bold`, scaled up) so the header carries real typographic presence; desktop nav links group inside a subtle bordered/tinted capsule (Surface/Border tokens) with CONTACT promoted to a solid champagne CTA pill outside the capsule (RESUME still omitted — destination unresolved). To keep the two from competing, the Hero's giant repeated-name treatment is cut sharply in scale (desktop max ~10rem → ~5.75rem) so the hero's dominant weight shifts to the positioning line; existing provisional positioning copy retained unchanged pending the final statement. | Decided |
| 32 | 2026-08-21 | **Home hero portrait treatment v2 (supersedes #30; narrows it further — the portrait asset remains approved and in the Home hero):** The plain radial-masked plane is replaced by a **duotone editorial treatment** tying the photo into Obsidian + Champagne: an Obsidian gradient bed acts as the duotone shadow end, the grayscaled photo is composited via `mix-blend-luminosity` so shadows melt into Obsidian, and a low-opacity (~30%) champagne `soft-light` grade warms highlights — accent discipline maintained. Composition is an **asymmetric editorial crop**: a taller `aspect-[3/4]` frame with a single diagonal cut across the top-right corner (`clip-path` polygon), bleeding past the hero grid's right edge on desktop (`lg:-mr-6`/`xl:-mr-10`, contained by the hero's `overflow-hidden`) and rising slightly above its cell (`lg:-mt-6`); mobile stays contained and centered with the same corner cut. A **one-time CSS load reveal** (frame wipes open via inset clip-path while the plane settles from scale 1.055→1) plays once on load; afterwards the portrait is fully static — no parallax, no sway, no pointer-follow. The reveal is defined only under `prefers-reduced-motion: no-preference`; reduced-motion visitors see the final state immediately. The component keeps zero client-side JS; `priority` migrated to Next.js 16's `preload`. | Decided |
| 33 | 2026-08-21 | **Ghost-typography signature motif + evidence visualization + scroll-aware nav (Home patterns):** (a) The Interlude's giant ghost chapter word is extracted into a reusable `GhostType` primitive (oversized display type at `text-ink/[0.05]`, slow scroll drift, aria-hidden/decorative, reduce-gated). It is a recurring-but-sparse signature device with **exactly three instances on Home**: Interlude ("Profile"), Selected Work's secondary project index (ghost word "Index"), and the Profile pull-quote (ghost quotation glyph). A fourth instance requires a new logged decision. (b) One bounded interactive evidence visualization (`EvidenceChart`, Recharts) added to Selected Work: a horizontal bar comparison of best reported results — Agri-Bot 96% accuracy · PioPetro up to 87% R² · ITI up to 97% F1 · D-HUB 96% accuracy — each bar labeled with its own metric kind; "up to" qualifiers preserved verbatim. Data lives statically in `src/content/evidence.ts`, traceable to CONTENT.md CONFIRMED entries (CONTENT.md's publish-time re-verification note still applies); no network dependency, so no loading/error state is possible. Keyboard-operable via focusable row overlays; a visually-hidden data table carries values/context/sources for screen readers; entrance animation disabled under reduced motion; champagne reserved for the active/emphasized state. (c) Header gains scroll-aware nav highlighting on Home (IntersectionObserver over `#work/#about/#experience/#publications/#contact`); the highlight reuses the existing champagne active convention and is visual-only — `aria-current` remains strictly route-based. | Decided |
| 34 | 2026-08-21 | **Art-directed transparent portrait pair (supersedes the #32 treatment; the portrait remains approved and in the Home opening):** The duotone editorial treatment is retired. The user supplied two background-removed cutout exports of the approved photo — `personal-image-desktop.png` (365×684) and `personal-image-mobile.png` (394×634) — which replace `yousef-portrait.jpeg` (asset removed from `public/portrait/`; raw JPG sources archived in `projects&certificate_images/`). Presentation is now the clean cutout at its native aspect ratio per viewport, rendered via `<picture>` art direction using Next.js 16's `getImageProps` pattern: the desktop crop serves ≥761px viewports, the mobile crop below (matched to the portal archive's 760px breakpoint); no gradient bed, no luminosity/soft-light grades, no radial edge melt, no corner clip-path. The one-time reduce-gated load reveal (inset clip-path wipe + scale settle) is kept. Because `<picture>` art direction cannot use `preload` without double-fetching, LCP priority moved to `loading="eager"` + `fetchPriority="high"` on the fallback image (the docs-recommended substitute). The unmounted editorial `Hero`/`PortraitObject` path was migrated to the same assets and pattern so it stays consistent if reintroduced. | Decided |
| 35 | 2026-08-21 | **Header scroll-progress hairline:** A 1px champagne progress hairline sits at the header's bottom edge (`scaleX` driven by document scroll fraction, `origin-left`, `pointer-events-none`). It reuses the existing champagne accent discipline and is purely visual — it announces reading position only and carries no semantic role or aria semantics. | Decided |
| 36 | 2026-08-21 | **Mobile header/nav treatment (Rekorder-inspired, translated to project tokens):** On mobile/tablet (<lg) the header wordmark is horizontally centered (3-column grid `grid-cols-[1fr_auto_1fr]`; hamburger stays right; desktop `lg:flex justify-between` untouched) and the mobile menu becomes an inset rounded card — Surface background (`bg-surface/95 backdrop-blur-xl`), Border hairline, **`rounded-2xl` (16px)** adopted as the first panel-scale radius: the codebase previously had only `rounded-full` (pills/dots) and the radius system was OPEN, so Tailwind's standard 16px step is taken as the panel counterpart to the pill language rather than a custom value. Items render as a 2-column grid of all six nav entries (CONTACT remains item six, href unchanged), compact mono uppercase (`text-[10px] tracking-[0.18em]`, ButtonLink's existing tracking so "PUBLICATIONS" fits at 320px) with a 4px dot bullet (`h-1 w-1 rounded-full`, existing dot proportion); active/hover shift text+dot to champagne only — accent discipline maintained. Escape-close/focus-return, scroll lock, aria attributes, and per-link close handlers are preserved unchanged from the prior QA pass. | Decided |
| 37 | 2026-08-22 | **Header seam regressions fixed — desktop bar borderless in all states + mobile open menu as ONE shape (amends #36's panel structure; visual language of #36 retained):** (a) Desktop ≥lg: the scrolled-state `border-b border-line` hairline is suppressed (`lg:border-transparent`) so the desktop bar paints no full-width line beneath it in any state — pixel A/B vs pre-pill HEAD proved that hairline predates the mobile work, but per user instruction the desktop bar is now clean/borderless while keeping the frosted `bg-obsidian/70 backdrop-blur-xl` scrolled treatment and the #35 progress hairline. All open-state bar styling (`bg-surface`, transparent border, no-transition) is scoped below-lg with explicit `lg:` restores, so a menu opened <lg that persists across a window resize can never restyle the desktop bar (the actual leak behind the reported desktop seam). (b) Mobile <lg: the open state is ONE structural element — an outer `motion.div` owns the only background (ink→Surface color tween), border-radius (pill→16px tween) and box-shadow (single dual-layer shadow, morphing between pill/panel values); the persistent trigger `<motion.button layout>` becomes a transparent geometry-only zone inside it (shared-layout morph preserved, focus-return intact); the nav grid is an internal zone with ZERO gap, no independent bg/border/shadow, joined by a subtle Border-token hairline divider INSIDE the shape; wordmark glyphs crossfade dark↔light as the shape's background flips ink→Surface. Framer cannot tween between `var()` endpoints, so the two shape colors are literal token mirrors in the component (keep in sync with globals.css). Escape-close/focus-return/scroll-lock/aria wiring unchanged. Verified structurally (CDP computed styles + pixel-row scans + simulated key events, 22/22 assertions) plus production build/tsc/eslint; final visual sign-off is manual. | Decided |
| 38 | 2026-08-22 | **Mobile header refinement pass — persistent light bar + light dropdown panel + theme-toggle stub (amends #36/#37's dark-panel treatment; user-directed):** (a) **Icon controls:** a theme toggle stub (Lucide `Sun`/`Moon`, local icon-state flip only — no real theming applied yet; aria-label alternates "Switch to light mode"/"Switch to dark mode") joins the speaker chip in the mobile bar; both use the identical raised-chip convention (`h-9 w-9 rounded-full border bg-raised`, champagne only in the active state) and each renders exactly once, inside the persistent bar so they stay visible with the menu open. The speaker's absolute-overlay placement is retired. Desktop nav untouched this pass (theme toggle intentionally not added there). (b) **Light panel:** the dropdown panel background moves from Surface (#14161A) to the same Ink token (#F3F0E8) as the closed pill — the bar no longer tweens background at all (constant Ink in both states), so mid-transition color flash is structurally impossible and the wordmark crossfade is removed (the dark variant serves both states). Active item inverts to a Surface pill with Ink text + champagne dot (16.4:1 / 8.0:1); inactive items move to Obsidian text on Ink (17.7:1 — Muted fails at 2.1:1) with Border-tone dots warming to champagne on hover; amends #36's champagne-text active convention for the mobile panel. (c) **Radius:** the full-pill radius is retired on mobile — `rounded-2xl` (16px, the #36 precedent) now applies to the bar in BOTH states and the panel; one value across shapes. (d) **Open-state shrink:** the bar morphs full-width ↔ hug logo+controls (`justify-self-stretch`↔`justify-self-center`, trigger `flex-1`↔`flex-none`) via the existing Framer `layout`; the menu expands below it as its OWN sibling shape (same Ink token/radius/shadow family, `w-[min(100%,20rem)]`, centered, 8px gap), replacing #37's single-shape structure. Escape-close/focus-return/scroll-lock/per-link close unchanged; click/tap-outside close added (`pointerdown` listener gated on the open state, ignoring targets inside bar/panel); reduced-motion behavior unchanged via the global `MotionConfig reducedMotion="user"` + CSS reduce block. Verified: production build/`tsc --noEmit`/ESLint clean; SSR assertions (one speaker chip per breakpoint, one theme toggle, `lucide-sun`=1/`lucide-moon`=0, single menu trigger, light-bar fallback class present, panel absent while closed); contrast ratios computed from token values; final visual sign-off manual. | Decided |
| 39 | 2026-08-22 | **Final positioning statement confirmed (Home "01 — SIGNAL / INTENT"):** After reviewing storytelling alternatives, the user retained the ORIGINAL positioning sentence in `PortalArchive.tsx` over the interim evidence-first variant that had replaced it: "I build the systems beneath a useful interface: models, data, and decisions brought into the same room." Rendered with the component's established single-accent pattern — `<em>models, data, and decisions</em>` (champagne via `.portal-statement em`, the same treatment the replaced span used) — applied to the sentence's natural thesis contrast. Kicker, role line, ghost numeral, and signal schematic untouched. Resolves the MISSING "final positioning statement" item in CONTENT.md "Professional positioning"; CONTENT.md updated accordingly. | Decided |
| 40 | 2026-08-22 | **Real light/dark theming — approved light palette + dark default (Phase 1: infrastructure + Header proof case):** The site gains genuine light/dark theming on runtime CSS custom properties. **Approved light palette** (mirrors the seven approved dark tokens, #17): Background `#F3F0E8`, Surface `#EAE6DC`, Primary text `#14161A`, Secondary text `#6E6E66`, Accent `#8C6A2E`, Accent highlight `#C9A86A`, Border `#DDD8CC`. In light mode the darker `#8C6A2E` is required for text/interactive accents (WCAG AA); the brighter `#C9A86A` is permitted only for decorative non-text accents. The implementation-only 8th token Raised (`#1A1D22`, not in the approved table) takes the derived light value `#E1DCCD` (one step beyond Surface along the same direction dark-mode Raised steps) — **provisional, pending user ratification**. **Behavior:** first visit always renders DARK (`prefers-color-scheme` is never read — the dark look is the brand, not a system echo); an explicit toggle choice persists in `localStorage["theme"]` with exact-value validation ("dark"/"light" only) and try/catch guards. **Mechanism:** all eight color tokens became runtime variables `--tk-*` scoped by `[data-theme="dark"]`/`[data-theme="light"]` in globals.css; Tailwind v4's color names map onto them via `@theme inline`, so every existing utility class (`bg-surface/70`, `text-muted`, …) re-themes automatically when `<html data-theme>` flips — opacity modifiers resolve at runtime via `color-mix(in oklab, …)`. `color-scheme` and `scrollbar-color` are declared per theme; `::selection` and `:focus-visible` are tokenized. SSR renders `data-theme="dark"` plus an inline pre-paint init script in `<head>` (localStorage read → attribute set, guarded) so a stored light choice applies before first paint with no flash and no hydration mismatch (`suppressHydrationWarning` on `<html>`). `src/lib/theme.ts` is the single runtime source of truth (`applyTheme` also syncs `<meta name="theme-color">` #0B0C0E/#F3F0E8); the `colorScheme:"dark"` viewport export was removed in favor of CSS-declared `color-scheme`. Header theme state subscribes via `useSyncExternalStore` + MutationObserver on the attribute (eslint-config-next's `react-hooks/set-state-in-effect` rule forbids effect-driven setState). **Scope:** Phase 1 = infrastructure + wiring BOTH existing Sun/Moon toggle stubs (desktop icon button + mobile raised chip, dynamic aria-labels preserved) + `Header.tsx` converted as the proof case — wordmarks swap by theme for legibility (`logo-wordmark.png` ↔ `logo-wordmark-dark.png`; reversed on the mobile ink bar) and the CONTACT pill hover pairing adds `hover:text-ink` only in light mode (cream-on-champagne-light ≈2:1 otherwise). The `.portal-home` Home content system still hardcodes dark hexes until the Phase 2 sweep converts remaining sections one at a time; `ButtonLink.tsx`/skip-link champagne hovers are known light-mode casualties deferred to that sweep. **Measured light-mode WCAG contrast (real composites):** muted nav text vs capsule composite 4.24:1, active nav accent vs raised pill 3.73:1, CONTACT cream-on-bronze pill 4.37:1 — all AA-large/non-text compliant yet below the 4.5:1 small-text threshold while using approved values exactly (**flagged for user ruling, not silently altered**); muted vs plain cream 4.51:1 passes AA-small; CTA hover pairing 8.01:1 (AAA); dark-mode baseline ≥7.48:1. Verified headlessly (puppeteer-core + Edge against a production build): fresh-session dark default, toggle flips attribute/body/meta + persists across reload, init-script-only application proven with all app JS blocked, desktop + mobile computed-style audits, production build/`tsc`/ESLint clean. | Decided |

| 41 | 2026-08-23 | **Theming Phase 2 — light-mode contrast rulings applied + Raised ratified + full Home theming sweep:** (a) The user ratified the derived light-mode Raised value `#E1DCCD` as-is (no change; Phase-1 “provisional” status resolved). (b) For the three light-mode sub-AA pairs flagged in Phase 1, the user ruled: fix by adjusting the specific usage/shade — approved token hexes untouched globally. Implemented as three new derived implementation tokens whose DARK values are byte-identical mirrors of the approved tokens (zero dark-mode visual change): `--tk-muted-strong` light `#65655D` for small muted text on tinted composites (capsule nav measured 4.24→4.85:1); `--tk-champagne-strong` light `#7A5B23` for small champagne text (active nav on raised pill 3.73→4.69:1; kickers/labels/links 4.37→5.03–5.51:1); `--tk-on-accent` light warm near-white `#FFFDF7` for labels resting on accent fills (CONTACT pill / portal actions 4.37→4.90:1). Direction note: on the mid-tone bronze `#8C6A2E` darkening can never pass AA-small (pure black tops out at 4.22), so the on-accent fix legitimately LIGHTENS instead. All pairs render at 10px mono → AA-small (4.5) applies; large display accents keep regular `#8C6A2E` (AA-large 3.0). Re-measured headlessly against rendered composites (translucent layers composited from live computed styles), not flat swatches. Footer column labels/hovers and the skip-link focus pairing swept under the same standard. (c) The full `.portal-home` Home content converted from pinned dark hexes to the `--tk-*` system (local `--p-*` mirrors now resolve runtime tokens), section-by-section with per-section headless verification (dark+light, mobile+desktop): hero, statement (+ signal schematic SVG strokes routed through theme vars), selected-work deck (wash scrim via color-mix on Background so card overlays follow the theme; overlay text uses strong/on-accent shades), roster, experience (Surface bg; muted alias re-scoped to strong there), publications, final CTA, footer. Decorative dark-tuned elements got light-specific adjustments only where they failed on cream: hero glow/stage alphas stepped down, action hover shadow softened, date-row hover/active tints rebased to bronze alphas, certificate plate given a whisper of ink-alpha elevation in light mode only (white paper cannot tonally pop on cream — interior-vs-bg luminance delta 150 dark vs 14 light). No layout, spacing, copy, or motion changes. | Decided |

| 42 | 2026-08-23 | **PHASE 4B — WORK page implemented (user-directed milestone; resolves the long-open "which old-site-only projects to include" question):** (a) **Scope:** `/work` implemented with SEVEN projects in a user-directed two-tier curation, and exactly ONE detail page (`/work/movenue`); the six other project detail pages remain an explicitly separate follow-up milestone (PHASE 10's full scope stays open). Primary tier (numbered 01–05): Movenue flagship (full-width feature treatment, dominant screenshot, champagne "Read the case study"), then Agri-Bot, PneumoScan, Personal RAG Chatbot, FMCG Pipeline as alternating asymmetric two-column rows with metrics strips — deliberately non-uniform so no card wall reads. Secondary tier ("Further builds", numbered 06–07): PotatoScan + Steganography Detector via `SecondaryProjectRow`, labeled honestly as slimmer-documentation entries pending verification rather than presented as equally proven. This resolves CONTENT.md's PROVISIONAL inclusion question for these two only; Ball Tracking Robot, Breast Cancer AI, ML Projects collection, SQL Projects stay excluded. (b) **Data layer:** `src/content/projects.ts` gains a `WorkProject` type + ordered `workProjects` (the Movenue entry derives name/role/timeframe/tags/image from `featuredProject` to avoid duplication), a narrow `WorkIndexEntry` type + `workIndexProjects` (timeframe optional — both entries lack one), and `movenueCaseStudy`; existing exports untouched (Home unaffected); `SecondaryProjectRow` prop narrowed from `Project` to `WorkIndexEntry`. (c) **Routing:** `src/app/work/[slug]/page.tsx` pre-renders ONLY `{slug:"movenue"}` (`generateStaticParams` + `dynamicParams = false` → every other slug 404s); metadata title "Movenue — Case study"; case-study structure per PHASE3_UX §7 (breadcrumb back-link, hero header with role/timeframe/status/stack meta grid + solid-champagne "Visit Movenue" pill as the page's single primary CTA, full-width masked-reveal screenshot, Overview / Role & ownership / Approach (Booking engine · Product surfaces · Quality gates · Platform) / Evidence sections, closing nav to All work + Contact pill + email). NO next-project link — with only one detail page it would dead-end. (d) **Demo-capture videos corrected BEFORE shipping:** planning had assumed four mp4 captures under `public/videos/` (Pneumonia, RAG assistance, FMCG pipeline, potato scan) and a click-to-load player component was built for them; verification found NO video files exist anywhere in the repo or git history — the earlier investigation note was wrong. All video wiring was removed (player deleted, no `video` fields shipped); entries render static captures only, and PneumoScan (no image asset either) renders a quiet labeled frame instead of fake media. A demo-capture player can be reintroduced when real assets are provided. (e) **GhostType not used on WORK** — the #33 three-instance motif stays Home-scoped by restraint. (f) **Content integrity:** every metric/date/tag/link verbatim from `yousef-portfolio-content.md` §6 — Agri-Bot 20 FPS / 96% accuracy / IUGRC 2024 (MobileNetV2 per resolved conflict), PneumoScan 5,863 X-rays / 89.3% F1 / 100% pneumonia recall with headline metrics only (the source-anomalous "85.1–85%" figure and dual-date "May 2023 / Apr 2026" timeframe are rendered verbatim where shown and remain flagged in CONTENT.md for user correction), RAG qualitative-only (no invented metrics), FMCG four-task Medallion story, Steganography "90% classification accuracy" kept on the secondary index line. Only real links used (Movenue live URL + six GitHub repos; no Movenue repo URL exists in the source). (g) Verified: production build clean (`/work` static, `/work/movenue` SSG), `tsc --noEmit` + ESLint clean, SSR content assertions (metrics/links/404 handling/UTF-8 integrity/Home regression), asset availability checks, desktop+mobile screenshots for manual sign-off; final visual review by user pending. | Decided |
| 43 | 2026-08-23 | **PHASE 4B — WORK page compositional redesign + leaked-copy fix + dormant demo-video infrastructure (amends parts of #42's presentation; scope/curation/routing unchanged):** (a) **Six distinct compositions replace per-entry template reuse** (user-approved in planning): Movenue keeps the dominant feature treatment but its media now breaks out as a full-bleed band — the page's ONLY full-bleed element, `overflow-hidden`-contained so the reveal-settle scale can never create a horizontal scrollbar; Agri-Bot becomes a hardware-dominant asymmetric grid (`lg:grid-cols-[1.15fr_0.85fr]`) with a vertical hairline-separated metrics rail; PneumoScan is a metrics-forward typographic band with NO media (no image asset exists — none invented); RAG Chatbot is a quiet UI-forward smaller-scale text-left/media-right mirror; FMCG is dashboard-led — a full-width `<figure>`+`figcaption` strip where the figcaption carries the sourced pipeline description and a large metric sits beside it; the secondary tier renders as calm numbered rows. Shared primitives extracted to `src/components/work/entry-parts.tsx` (`StatusChip`, `EntryHeader`, `EntryMedia`, `TagLine`, `MetricFigure`, `EntryActions`); the old uniform template component was deleted. (b) **Leaked internal-process copy removed from visitor UI:** #42's secondary tier had rendered process language ("slimmer documentation, fuller write-ups pending verification") and an index intro referencing internal evidence state. The tier is retitled "Additional builds", the intro line was deleted, and each row now shows a real one-line description sourced verbatim in substance from `yousef-portfolio-content.md` §6 (PotatoScan: CNN potato-leaf disease detector served full-stack, FastAPI behind React; Steganography Detector: CNN detecting hidden data in images, 90% classification accuracy). `WorkIndexEntry.evidence` became `WorkIndexEntry.summary`; `SecondaryProjectRow` renders it. The PROVISIONAL content status of these two projects is unchanged — only their on-page presentation stopped narrating internal process. (c) **Demo-video infra built DORMANT** (user decision "infra now, activate later"; supersedes #42(d)'s "player deleted" stance by rebuilding it properly for when assets exist): optional `ProjectVideo {mp4, webm?}` field on `WorkProject`, unset everywhere; `HoverVideo` client component mounts `<video muted loop playsinline preload="none">` over the poster only on interaction intent — hover-in/out on fine pointers, always-visible tap affordance on coarse pointers, real focusable `<button>` (Enter/Space) with `aria-pressed`, `prefers-reduced-motion` blocks hover autoplay while keeping explicit controls, and any load/playback failure reverts to the poster image. Encoding spec for future assets: H.264 MP4 CRF ~29, long edge ≤1280, faststart, muted/no-audio, 3–8 MB target, dropped into `public/videos/`; then set the entry's `video` field. (d) **Verified:** production build clean (`/work` static, `/work/movenue` SSG), `tsc --noEmit` + ESLint clean; 18/18 headless composition×breakpoint checks at 375/820/1440 (section presence, no horizontal overflow, no clipped children); 18/18 HoverVideo behavioral assertions against a production server using genuinely decodable WebM fixtures generated in-browser (resting dormancy incl. zero eager video mount + hidden control on fine pointers; hover→mount→actually-playing with advancing currentTime; leave→unmount→poster restored; keyboard Enter toggle both ways with visible focused control; reduced-motion gating verified with emulation confirmed active; touch tap-to-play; real-404 → failed state → poster fallback intact); leak scan for all former process phrases returns zero matches; Home regression checked. Final visual sign-off manual. | Decided |
| 44 | 2026-08-23 | **PHASE 4B — WORK expanded to full index grid + ALL SEVEN project detail pages (supersedes #42/#43's "one detail page" scope; curation of seven projects unchanged):** (a) **Index recomposed as a grid:** the #43 six-distinct-compositions scroll is replaced by one full-width flagship composition (Movenue: numbered header, capture linking into `/work/movenue`, summary + verbatim metric lines, champagne "Read the project page") followed by a uniform responsive card grid (sm:2 / lg:3 columns, no holes at either breakpoint) for the remaining six builds; each tile links to its own detail page and carries number/status/name/kind/sourced summary/timeframe. Tiles without a capture render a quiet labeled panel — no imagery invented; tiles with encoded demos embed `HoverVideo` directly in the tile. Old-site-only builds remain included per the resolved question (#42). `workProjects`/`WorkProject`/`movenueCaseStudy`/`workIndexProjects` deleted from the data layer; `entry-parts.tsx` and `MovenueCaseStudy.tsx` deleted; new shared pieces are `MetricFigure.tsx` (extracted), `tech-icons.tsx`, and the data-driven `ProjectDetailPage.tsx`. Home's Selected Work unaffected (`featuredProject`/`secondaryProjects` untouched; `WorkIndexEntry` type retained). (b) **All seven detail pages live** via one data-driven template over `projectDetails: ProjectDetail[]`: Problem → Methodology → Solution → Results → Stack → Links → prev/next nav in curated order, breadcrumb always present. Short pages render ONLY the sections their sourced content supports — PotatoScan (Problem/Stack/Links) and Steganography Detector (Problem/Results/Stack/Links) stay honest instead of padded. `generateStaticParams` covers all seven slugs with `dynamicParams = false`; per-slug metadata from `summary`. Demo captures play inside the Solution section where they exist. (c) **Real demo videos shipped:** three user-provided raw AVI captures found in `public/videos/` ("Agribot - demo.avi", "FMCG Data Engineer - demo.avi", "Ai_personal_assistance - demo.avi" — the last confirmed by the user to be the Personal RAG Chatbot demo) were re-encoded to H.264 MP4 (CRF 28, preset medium, long edge ≤1280, yuv420p, faststart, audio stripped) + JPEG poster stills: `agribot-demo.mp4` (5.9 MB), `fmcg-pipeline-demo.mp4` (1.3 MB), `rag-chatbot-demo.mp4` (3.97 MB); WebM siblings came out larger than MP4 and were dropped ("where feasible" scoped to MP4-only). Raw AVIs left untouched on disk. `HoverVideo` gained an optional explicit `aspect` prop so landscape clips behind differently-shaped posters hold one stable frame. (d) **Tech-stack icon row added** (`@icons-pack/react-simple-icons@13.15.1`, tree-shakeable named imports — new dependency, recorded in TECH_STACK.md): brand marks only where a mark exists and the tech is sourced; concepts without marks (Recharts, Delta Lake, Unity Catalog, Groq, ChromaDB, custom CNN, transfer learning, star schema…) render neutral Lucide glyph chips instead. Every rendered entry maps to the project's confirmed stack. (e) **Media presentation:** real captures sit inside a pure-CSS laptop bezel (`LaptopFrame`) on detail pages; PneumoScan/PotatoScan/Steganography get quiet labeled panels (no assets exist — none invented). (f) **Content integrity held:** every figure/description/tag/link traces to `yousef-portfolio-content.md` §6 including known anomalies rendered verbatim (PneumoScan "85.1–85%", dual-date timeframe — still flagged in CONTENT.md); Movenue still has NO repo link (only the live URL). (g) Verified: production build clean (`/work` static + all seven `/work/[slug]` SSG paths), `tsc --noEmit` + ESLint clean; prerendered-HTML assertions passed for all eight routes (metrics, links, video sources, section presence AND absence on short pages, no leaked process copy). Final visual sign-off manual by user pending. | Decided |

| 45 | 2026-08-23 | **PHASE 4B fix pass (user visual review) — card corner-radius extended to WORK cards + video interaction REVERSED to click-to-play-with-audio (supersedes #43(c)/#44's hover-autoplay-muted player behavior):** (a) **Radius:** `rounded-2xl` (16px, reused exactly per the #36/#38 precedent — no new value invented) extended beyond its original mobile-nav scope to become the working card-scale radius: applied to the Movenue flagship media block and all six ALL PROJECTS tiles on `/work` (with `overflow-hidden` so children clip). The formal radius system remains a Phase 5 deliverable. (b) **Video reversal:** the hover-triggered muted-autoplay interaction is removed entirely. Reason: the demo captures' voiceover audio is substantive content and muting defeated its purpose; hover autoplay also started motion without explicit consent. Replacement behavior (`DemoVideo`, replaces `HoverVideo`; hover-hide CSS deleted): static poster renders by default everywhere a video exists; a clearly visible play affordance (real focusable `<button>`) is shown in every state; playback starts ONLY on that button's click handler and plays WITH audio (never muted by the player); while active, Play/Pause toggle + an explicit close control return the frame to its static poster state (playback end does the same); load/playback failure reverts to poster. No autoplay path exists under any condition (mount-on-click only; `play()` called solely from user-gesture handlers); keyboard users operate playback via Tab + Enter/Space like pointer users. Structural consequence: index tiles were restructured from anchor-wrapped cards to a stretched-link pattern (card = positioned container, title `<Link>` covers it via inset pseudo-element) so the video controls are real buttons rather than invalid button-inside-anchor nesting. (c) **Audio asset correction (verified before implementing):** container-level scan of the three served MP4s found NO audio track (consistent with #44(c)'s "audio stripped"), while the raw AVI sources carry audible MP3 stereo (volumedetect mean −15.8/−29.3/−32 dB) — unmuted playback of the old encodes would have been silent. All three MP4s re-encoded FROM THE AVIs preserving audio (H.264 CRF 28 preset medium, ≤1280px long edge, yuv420p, faststart + AAC 128k stereo; agribot 7.3 MB, fmcg 3.4 MB, rag 5.9 MB), each verified post-encode as carrying a non-silent AAC track. ffmpeg was used as a temporary external tool in the OS temp workspace only — not a project dependency. (d) Also in this pass (recorded for completeness): Movenue flagship capture contained at `max-w-4xl` centered (reduced from full-container dominance, still the page's largest visual), and the ALL PROJECTS grid moved to 2 columns from the smallest breakpoint with half-width reflow handled inside the existing type scale. (e) Verified: production build clean, `tsc --noEmit` + ESLint clean; headless suite 30/30 on the production server (fresh-load zero `<video>` mounts across `/work` + all three video detail routes; trusted-pointer click → playing with `muted=false volume=1` and advancing currentTime; pause holds; resume; close → poster + focus return; keyboard Enter playback; in-place tile playback without navigation; stretched-link navigation intact; 2-col grid at 375/390/430 with zero horizontal overflow/clipped text; flagship 896px centered at 1440, full column at 375; computed 16px radius ×7 surfaces; Home regression; zero console/page errors). Final visual sign-off manual by user pending. | Decided |

| 46 | 2026-08-23 | **Masked line reveal extracted to shared primitive `MaskedLineReveal` + reused for the WORK hero entrance:** The Home hero's masked per-line name-reveal technique (overflow-hidden clip spans with descender compensation, staggered framer-motion rise `y 112%→0%`, 1.05s, ease `cubic-bezier(0.22,1,0.36,1)`, reduce-gated via component-level `useReducedMotion` initial-false + global `MotionConfig reducedMotion="user"` + CSS reduce block) is extracted byte-for-byte into reusable `src/components/ui/MaskedLineReveal.tsx`; the unmounted editorial `Hero.tsx` now consumes it so its DOM/behavior stay identical if reintroduced. First reuse: the `/work` hero (`WorkHero.tsx`) — the "Work." H1 enters via the primitive while the project-count/date-range metadata and intro paragraph settle in afterward using Home's existing `item` fade/rise language (delays 0.78s/~0.92s); H1 stepped up one existing Tailwind token at desktop only (`lg:text-9xl`). No new timing curve or motion pattern introduced. | Decided |

| 47 | 2026-08-23 | **PHASE 4B correction — orphaned hero top rule removed (logs that #45's kicker-removal pass was incomplete; #45's other rulings unaffected):** When #45 removed the redundant "WORK" kicker above the H1 it deliberately kept "the bare single top rule as the header frame" (`<div className="border-t border-line" />` at the top of `WorkHero`). User visual review flagged that rule as an orphaned hairline floating in the empty space between the fixed header and the "Work." heading — CORRECT: a site-wide audit found it is the ONLY content-less hairline in the codebase (every other `border-t` pairs with a mono wayfinding label per the SectionFrame convention or with immediately-following content), and Home's live hero (`PortalArchive`) carries NO rule above it — Home's hairlines begin only at indexed sections. The rule is therefore removed so `/work` flows header → hero content directly, with the page's first hairline being the flagship Movenue frame below the hero — matching Home's established pattern. Hero copy, entrance animation (#46), spacing rhythm (`pt-36/sm:pt-44`, `mt-16/sm:mt-20`), Movenue section, grid, and all lower rules untouched. Verification note: an A/B headless probe against pre-fix HEAD proved reduced-motion mount behavior byte-identical before/after (a transient hidden state of ~8–22 frames at hydration under emulated `prefers-reduced-motion`, settling fully opaque/seated) — pre-existing since #46, NOT introduced by this pass, left as-is (animation explicitly out of scope here) and flagged for user awareness. | Decided |

## Rules

- Do not treat proposals as decisions.
- Do not silently reverse a decision — record the change here with a new row and date.
- Open items (11, 12, 20–26) must be resolved deliberately in the appropriate phase per [ROADMAP.md](./ROADMAP.md) (typography/logo → Phase 5 Brand System; wireframes/homepage composition → Phase 4; component inventory → Phase 6; motion choreography/3D concept → Phase 12), not incidentally during implementation.
